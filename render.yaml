services:
  - type: web
    name: sales-tools-2026-api
    runtime: node
    plan: starter
    rootDir: server
    buildCommand: npm ci && npm run generate
    startCommand: npm run start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: DATABASE_URL
        sync: false
      - key: TELNYX_API_KEY
        sync: false
      - key: SYNC_ON_STARTUP
        value: "true"
      - key: SYNC_LOOKBACK_DAYS
        value: "30"

  - type: web
    name: sales-tools-2026-client
    runtime: static
    rootDir: client
    buildCommand: rm -f package-lock.json && npm install --include=optional && npm install @rollup/rollup-linux-x64-gnu --no-save && npm run build
    staticPublishPath: dist
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: VITE_API_BASE_URL
        sync: false